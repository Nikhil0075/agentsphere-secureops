[project]
name = "agentsphere-secureops"
version = "0.1.0"
description = "A permissioned workforce of AI agents for SOC investigation, triage and verifiable response"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
addopts = "-q"
filterwarnings = ["ignore::DeprecationWarning"]
