# AgentSphere SecureOps — Phase 0 dependencies
# Python 3.13

# --- data layer ---
pandas>=2.2
pyarrow>=16.0
numpy>=1.26

# --- baseline model ---
# LightGBM is the primary baseline; app/models/baseline.py falls back to sklearn's
# HistGradientBoostingClassifier automatically if it is unavailable on the host.
lightgbm>=4.5
scikit-learn>=1.5
joblib>=1.4
matplotlib>=3.8

# --- contracts / validation ---
pydantic>=2.7
pydantic-settings>=2.3
PyYAML>=6.0
python-dotenv>=1.0

# --- agents / LLM ---
openai>=1.40
openai-agents>=0.19,<0.20

# --- retrieval: BM25 + FAISS, fused with RRF ---
# faiss-cpu is IndexFlatIP only here, which is exact cosine over L2-normalised vectors. At 5,000
# documents that is the right choice; an HNSW index would be a more complicated way to get a
# worse answer.
rank-bm25>=0.2.2
faiss-cpu>=1.9

# --- api (the React frontend lives in frontend/) ---
fastapi>=0.110
uvicorn[standard]>=0.29

# --- proof layer ---
eth-hash[pycryptodome]>=0.7
web3>=7.0
eth-account>=0.13

# --- dataset acquisition ---
kagglehub>=0.3

# --- tests ---
pytest>=8.2
